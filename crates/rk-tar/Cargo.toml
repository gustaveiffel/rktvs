[package]
name = "rk-tar"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"
description = "Tar archive ingest and export for rk"
repository = "https://github.com/rktvs/rk"

[dependencies]
rk-core = { path = "../rk-core" }
tar = "0.4"
tracing = "0.1"

[dev-dependencies]
tempfile = "3.25"
